#!/bin/sh -e

case "$1" in
  configure|abort-*)

    mktexlsr
    echo "Creating missing formats."
    update-updmap --quiet
    ;;
  *)
    echo "postinst called with unknown argument: '$1''" >&2
    ;;
esac


#DEBHELPER#

